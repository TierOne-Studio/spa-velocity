# Public browser configuration. Never put secrets in VITE_* variables.
VITE_API_URL=http://localhost:3000

# Optional Airweave Connect SDK endpoint. Keep HTTPS outside localhost.
VITE_AIRWEAVE_CONNECT_URL=https://connect.airweave.ai
